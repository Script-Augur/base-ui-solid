---
'@script-augur/base-ui-solid': minor
---

Add Fieldset compound component (`Root`, `Legend`) with nested disabled inheritance and legend `aria-labelledby` association, ported from `@base-ui/react@1.7.0`.
