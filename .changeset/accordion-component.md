---
'@script-augur/base-ui-solid': minor
---

Add Accordion (`Root` / `Item` / `Header` / `Trigger` / `Panel`) with single/multiple open values, cancelable `onValueChange`, deep import `@script-augur/base-ui-solid/accordion`, and Storybook stories.
