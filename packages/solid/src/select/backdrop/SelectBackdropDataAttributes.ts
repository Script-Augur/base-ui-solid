/**
 * Present on the select backdrop element.
 */
export enum SelectBackdropDataAttributes {
  /**
   * Present when the corresponding select is open.
   */
  open = 'data-open',
  /**
   * Present when the corresponding select is closed.
   */
  closed = 'data-closed',
}
