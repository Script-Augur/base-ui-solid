/**
 * Present on select scroll arrow elements.
 */
export enum SelectScrollArrowDataAttributes {
  /**
   * Present when the arrow is visible (the list can scroll further in this direction).
   */
  visible = 'data-visible',
  /**
   * The side of the anchor the popup is placed on.
   */
  side = 'data-side',
}
