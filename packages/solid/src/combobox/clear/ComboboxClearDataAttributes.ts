/**
 * Data attributes for {@link ComboboxClear}.
 */
export enum ComboboxClearDataAttributes {
  /** Present when the clear button is visible. */
  visible = 'data-visible',
  /** Present when the component is disabled. */
  disabled = 'data-disabled',
}
