/**
 * Present on the combobox backdrop element.
 */
export enum ComboboxBackdropDataAttributes {
  /**
   * Present when the corresponding combobox is open.
   */
  open = 'data-open',
  /**
   * Present when the corresponding combobox is closed.
   */
  closed = 'data-closed',
}
