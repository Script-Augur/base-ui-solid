/**
 * `data-*` attribute names written by {@link ToolbarLink}.
 */
export const ToolbarLinkDataAttributes = {
  /**
   * Indicates the orientation of the toolbar.
   * @type {'horizontal' | 'vertical'}
   */
  orientation: 'data-orientation',
} as const
