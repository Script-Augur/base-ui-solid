/**
 * `data-*` attribute names written by {@link Toggle}.
 *
 * Catalog for docs and consumers styling or querying toggle state.
 */
export const ToggleDataAttributes = {
  /**
   * Present when the toggle button is pressed.
   */
  pressed: 'data-pressed',
  /**
   * Present when the toggle button is disabled.
   */
  disabled: 'data-disabled',
} as const
