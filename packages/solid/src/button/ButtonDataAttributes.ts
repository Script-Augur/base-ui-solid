/**
 * `data-*` attribute names written by {@link Button}.
 *
 * Catalog for docs and consumers styling or querying button state
 * (e.g. `[data-disabled]` / `ButtonDataAttributes.disabled`).
 */
export const ButtonDataAttributes = {
  /**
   * Present when the button is disabled.
   */
  disabled: 'data-disabled',
} as const
