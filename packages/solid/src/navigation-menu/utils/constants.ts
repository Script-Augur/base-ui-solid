/** Default delay (ms) before hover-open. Matches upstream Navigation Menu. */
export const OPEN_DELAY = 50

/** Default delay (ms) before hover-close. Matches upstream Navigation Menu. */
export const CLOSE_DELAY = 50

/**
 * Window after a hover-open during which a click will not close the menu
 * (upstream patient-click threshold).
 */
export const PATIENT_CLICK_THRESHOLD = 500

/** Attribute marking Navigation Menu triggers for outside-press filtering. */
export const NAVIGATION_MENU_TRIGGER_IDENTIFIER =
  'data-base-ui-navigation-menu-trigger'
