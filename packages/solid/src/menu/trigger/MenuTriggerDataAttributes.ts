/**
 * Data attributes for {@link MenuTrigger}.
 */
export enum MenuTriggerDataAttributes {
  /**
   * Present when the corresponding menu is open.
   */
  popupOpen = 'data-popup-open',
  /**
   * Present when the trigger is disabled.
   */
  disabled = 'data-disabled',
}
