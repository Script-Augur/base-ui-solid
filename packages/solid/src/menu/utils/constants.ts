/** Patient click threshold (ms) shared with Popover hover stickiness. */
export const PATIENT_CLICK_THRESHOLD = 500

/** Default trigger open delay (ms). */
export const OPEN_DELAY = 100

/** Typeahead buffer reset (ms). */
export const TYPEAHEAD_RESET_MS = 500
