/**
 * Data attributes for {@link MenuViewport}.
 */
export enum MenuViewportDataAttributes {
  current = 'data-current',
  open = 'data-open',
  closed = 'data-closed',
}

/**
 * CSS variables for {@link MenuViewport} (parity placeholder).
 */
export enum MenuViewportCssVars {}
