/**
 * Public package stub — component port starts after scaffold review.
 * Upstream pin: @base-ui/react@1.7.0 (see UPSTREAM.md).
 */
export const version = "0.0.1" as const

export const PACKAGE_NAME = "@script-augur/base-ui-solid" as const
