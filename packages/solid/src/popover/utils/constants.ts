/** Default rest delay (ms) before hover-open. Matches upstream `OPEN_DELAY`. */
export const OPEN_DELAY = 300

/**
 * Window after a hover-open during which a click will not close the popover
 * (upstream patient-click threshold).
 */
export const PATIENT_CLICK_THRESHOLD = 500
