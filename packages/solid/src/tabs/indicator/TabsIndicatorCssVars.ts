/**
 * CSS custom properties set by {@link TabsIndicator} for the active tab geometry.
 */
export const TabsIndicatorCssVars = {
  /** Distance from the list container's left edge to the active tab. */
  activeTabLeft: '--active-tab-left',
  /** Distance from the list container's right edge to the active tab. */
  activeTabRight: '--active-tab-right',
  /** Distance from the list container's top edge to the active tab. */
  activeTabTop: '--active-tab-top',
  /** Distance from the list container's bottom edge to the active tab. */
  activeTabBottom: '--active-tab-bottom',
  /** Width of the active tab. */
  activeTabWidth: '--active-tab-width',
  /** Height of the active tab. */
  activeTabHeight: '--active-tab-height',
} as const
