/**
 * `data-*` attribute names written by {@link TabTrigger}.
 */
export const TabTriggerDataAttributes = {
  /** Direction of tab activation relative to the previous active tab. */
  activationDirection: 'data-activation-direction',
  /** Layout orientation of the tabs. */
  orientation: 'data-orientation',
  /** Present when the tab is disabled. */
  disabled: 'data-disabled',
  /** Present when the tab is active. */
  active: 'data-active',
} as const
