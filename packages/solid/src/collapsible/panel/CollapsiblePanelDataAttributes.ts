/**
 * `data-*` attribute names written by {@link CollapsiblePanel}.
 */
export const CollapsiblePanelDataAttributes = {
  /** Present when the collapsible panel is open. */
  open: 'data-open',
  /** Present when the collapsible panel is closed. */
  closed: 'data-closed',
} as const
