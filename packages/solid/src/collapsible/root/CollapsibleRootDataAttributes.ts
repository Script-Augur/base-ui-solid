/**
 * `data-*` attribute names written by {@link CollapsibleRoot}.
 */
export const CollapsibleRootDataAttributes = {
  /** Present when the collapsible is open. */
  open: 'data-open',
  /** Present when the collapsible is closed. */
  closed: 'data-closed',
} as const
