/** Attribute marking the drawer content region (scroll / swipe targeting). */
export const DRAWER_CONTENT_ATTRIBUTE = 'data-drawer-content'

export enum DrawerContentDataAttributes {
  /**
   * Present on the drawer content container.
   */
  content = DRAWER_CONTENT_ATTRIBUTE,
}
