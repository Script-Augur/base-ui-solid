/** Default rest delay (ms) before hover/focus open. Matches upstream `OPEN_DELAY`. */
export const OPEN_DELAY = 600

/** Default delay (ms) before hover close. Matches upstream `CLOSE_DELAY`. */
export const CLOSE_DELAY = 300
