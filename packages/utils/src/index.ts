/**
 * Workspace stub — real helpers land in the core-primitives task.
 */
export const PACKAGE_NAME = "@script-augur/base-ui-utils" as const

export function noop(): void {
  // intentionally empty
}
